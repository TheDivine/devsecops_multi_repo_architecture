image:
  repository: <registry-url>/<project-name>/app
  tag: <image-tag>
  pullPolicy: IfNotPresent

replicaCount: 2

service:
  port: 8000

ingress:
  enabled: true
  controller: traefik
  host: <application-hostname>
  tls: true

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
