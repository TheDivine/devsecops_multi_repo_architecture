services:
  app:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      APP_ENV: development
      LOG_LEVEL: debug
    env_file:
      - .env.example

# Local development only. Production runtime configuration belongs in the deployment repository.
