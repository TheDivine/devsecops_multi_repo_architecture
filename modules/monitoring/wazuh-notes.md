# Optional Wazuh Integration

Use Wazuh when endpoint security monitoring or compliance telemetry is required.

## VM Clients

- install Wazuh agent through Ansible
- enroll against a private Wazuh manager
- monitor agent health in the monitoring repo

## Kubernetes Clients

Use Wazuh where it fits the organization model. Do not replace Kubernetes admission and runtime controls with SIEM visibility alone.
