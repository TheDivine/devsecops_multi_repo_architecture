# Scripts

Safe helper scripts for platform and template repositories.

- `defectdojo/`: report reimport helpers
- `policy/`: policy validation entrypoints
- `release/`: release and promotion helpers

Scripts use strict mode and expect secrets through environment variables.
